
# Write your MySQL query statement below
select a.query_name,
 round((sum(a.rating/a.position))/count(*),2)as quality,
round(sum(if(rating<3,1,0))*100/count(a.query_name),2)as poor_query_percentage
from Queries a
group by query_name;
